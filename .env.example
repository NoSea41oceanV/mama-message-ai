# External providers are optional in the current demo build.
# Keep every secret on the server. Never expose these values to public/app.js.
#
# ================================================================
# [実接続テスターが入力する場所]
# 1. ORCAROUTER_API_KEY      -> 必須（返答・安全判定）
# 2. ROUTER_PROVIDER         -> orcarouter に変更
# 3. SPEECH_PROVIDER         -> elevenlabs に変更
# 4. ELEVENLABS_API_KEY      -> 必須（録音からクローン作成・本人声TTS）
# 5. VIDEO_GENERATION_PROVIDER -> did に変更する場合のみ
# 6. DID_API_KEY             -> D-ID動画を作る場合のみ必須
#
# ELEVENLABS_VOICE_ID / ELEVENLABS_VOICE_PROFILE_ID は、アプリ画面から
# クローンを作る場合は空欄のままにしてください。
# OPENAI_*、STT_API_KEY、MEDIA_API_KEY、KLING_* は今回の確認では不要です。
# ================================================================

# [必須 1/4] OrcaRouterの秘密キー
ORCAROUTER_API_KEY=
ORCAROUTER_BASE_URL=https://api.orcarouter.ai/v1
# A small structured-output model keeps child-facing replies responsive.
ORCAROUTER_MODEL=openai/gpt-5.4-nano
ORCAROUTER_TIMEOUT_SECONDS=25
# [必須 2/4] 実接続では demo を orcarouter に変更
ROUTER_PROVIDER=demo

STT_API_KEY=
STT_PROVIDER=demo
STT_BASE_URL=https://api.openai.com/v1
STT_MODEL=gpt-4o-mini-transcribe

# Optional true guardian voice. Requires an OpenAI eligible-customer Custom Voice ID.
# The guardian must separately record OpenAI's prescribed consent phrase.
# Keep disabled until OPENAI_API_KEY and OPENAI_CUSTOM_VOICE_ID are provisioned.
# [必須 3/4] ElevenLabs本人声では disabled を elevenlabs に変更
SPEECH_PROVIDER=disabled
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_CUSTOM_VOICE_ID=
# Bind that voice to exactly one local guardian profile; prevents cross-profile use.
OPENAI_CUSTOM_VOICE_PROFILE_ID=
OPENAI_TTS_MODEL=gpt-4o-mini-tts-2025-12-15
OPENAI_TTS_FORMAT=mp3
OPENAI_TTS_TIMEOUT_SECONDS=15
# Short-lived in-memory cache: identical replies do not incur a second TTS call.
REPLY_AUDIO_TTL_MINUTES=30
REPLY_AUDIO_CACHE_ENTRIES=32
REPLY_AUDIO_MAX_BYTES=5242880

# Current practical voice-clone provider. With the API key configured, the app
# uploads an explicitly consented 30-second-to-2-minute recording, creates an
# Instant Voice Clone via API, and stores its Voice ID encrypted per profile.
# [必須 4/4] ElevenLabsの秘密キー
ELEVENLABS_API_KEY=
ELEVENLABS_BASE_URL=https://api.elevenlabs.io/v1
# Optional pre-created mapping. Leave both empty when the app creates the clone
# from its guardian registration screen; that Voice ID is encrypted per profile.
# [空欄のまま] アプリ内クローン作成後、暗号化領域へ自動保存されます
ELEVENLABS_VOICE_ID=
ELEVENLABS_VOICE_PROFILE_ID=
# Flash is the lowest-cost/lowest-latency Japanese-capable option.
ELEVENLABS_TTS_MODEL=eleven_flash_v2_5
ELEVENLABS_OUTPUT_FORMAT=mp3_44100_128
ELEVENLABS_TTS_TIMEOUT_SECONDS=15

MEDIA_API_KEY=
MEDIA_PROVIDER=demo
PREGENERATED_VIDEO_URL=

# Registered guardian photo -> D-ID free trial or Kling image-to-video.
# Disabled avoids external uploads and provider charges by default.
# [任意] D-IDを使う場合だけ disabled を did に変更
VIDEO_GENERATION_PROVIDER=disabled
# D-ID Studio > Account settings > API key. Paste API_USERNAME:API_PASSWORD as issued;
# an already-prefixed "Basic ..." value is also accepted. Trial credits are limited,
# and videos generated on the trial have a D-ID watermark.
# [任意] VIDEO_GENERATION_PROVIDER=did の場合だけ入力
DID_API_KEY=
DID_BASE_URL=https://api.d-id.com
# Provider-neutral limits. The KLING_* names below remain supported as fallbacks.
VIDEO_REQUEST_TIMEOUT_SECONDS=30
VIDEO_POLL_INTERVAL_SECONDS=5
VIDEO_POLL_TIMEOUT_SECONDS=600
VIDEO_DOWNLOAD_TIMEOUT_SECONDS=30
# Enforced before encrypted at-rest storage (25 MiB).
VIDEO_MAX_BYTES=26214400

# OrcaRouter Kling settings (used when VIDEO_GENERATION_PROVIDER=kling).
KLING_VIDEO_MODEL=kling/kling-v3
KLING_VIDEO_MODE=std
KLING_VIDEO_DURATION_SECONDS=5
KLING_VIDEO_REQUEST_TIMEOUT_SECONDS=30
KLING_VIDEO_POLL_INTERVAL_SECONDS=5
KLING_VIDEO_POLL_TIMEOUT_SECONDS=600
KLING_VIDEO_DOWNLOAD_TIMEOUT_SECONDS=30
KLING_VIDEO_MAX_BYTES=26214400

# Optional: 32-byte base64/64-char hex key for persistent guardian samples.
# When empty, a local key is generated under the gitignored .data directory.
SAMPLING_ENCRYPTION_KEY=

# Remote guardian chat view. Set a private code before sharing the app.
# Demo mode falls back to 2468 only when this value is empty.
GUARDIAN_VIEW_ACCESS_CODE=
# Conversation text is kept only in server memory and expires automatically.
CONVERSATION_TTL_MINUTES=30
# Keep 127.0.0.1 for one PC. Use 0.0.0.0 only on a trusted LAN or deployment.
HOST=127.0.0.1

# Recommended MVP limits from the requirements deck.
MAX_RECORDING_SECONDS=15
# The response flow reserves time for routing; media is capped at 18 seconds.
MEDIA_TIMEOUT_SECONDS=18
TECHNICAL_LOG_TTL_HOURS=24
